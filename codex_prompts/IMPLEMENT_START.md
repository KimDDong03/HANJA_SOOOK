구현 시작해.

먼저 루트의 AGENTS.md를 읽고 반드시 그대로 따라.
그다음 docs/06_CODEX_TASKS.md의 Ticket 00만 구현해.

이번 응답에서 할 일:
1. Ticket 00 구현에 필요한 파일만 생성/수정해.
2. pubspec.yaml이 없으면 `flutter create . --platforms=android,ios`로 Flutter 프로젝트를 먼저 초기화하고, 기존 docs/supabase/tools 파일은 유지해. 그 다음 기본 구조를 만들고 placeholder route들을 연결해.
3. Supabase URL/key는 --dart-define으로 받게 해. 절대 하드코딩하지 마.
4. Firebase는 절대 추가하지 마.
5. UI 화면에서 Supabase를 직접 호출하지 마.
6. Ticket 00 완료 후 flutter analyze 결과를 보고해.

Ticket 00이 끝나기 전에는 Ticket 01 이후 작업을 하지 마.
